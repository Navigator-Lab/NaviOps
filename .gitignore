# Secrets — never commit
.secrets/
**/.env
.env.*
.aws/
*.pem
*.ppk
id_rsa*
id_ed25519*
*.key
# Wazuh SIEM lab generated SSL material — private keys, never commit
NaviOpsSec/infra/lab/config/wazuh_indexer_ssl_certs/

# Private interview prep — never published (per EXP_REPORT_2026-06-11_Portfolio-Repo-Restart-and-Authenticity)
*.private.md

# Answer-key / reference solutions — kept LOCAL so the operator builds the scripts himself
# as practice (D18). Rebuild your own at scripts/ ; these are the "check my work" copies.
docs/learning/reference-solutions/
# stray Python bytecode
__pycache__/
*.pyc

# Internal Navi project-management docs — local memory only, not recruiter-facing
# (may reference personal/strategy details). docs/learning/ (the curriculum) stays public.
docs/reports/
docs/*.md
docs/learning/JOB_MILESTONES.md
docs/learning/JIRA.md
docs/learning/LEARNING_STATE.md
docs/learning/CLAUDE_TEACHING_RULES.md
docs/learning/prompts/
docs/learning/ROADMAP.md
# Terraform (per github/gitignore Terraform.gitignore)
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Navi internal state
.agent/state/active_project.json

# Backups & archives
_backup_*/
.agent/_backup_*/

# Node (future tooling)
node_modules/
dist/
*.tsbuildinfo

# Python build artifacts
__pycache__/
*.py[cod]

# OS / editor
.DS_Store
*.log
